<h2>
	<%= link_to '↞', root_path %> 
	Thought book
</h2>

<ul>
	<% @blogs.each do |blog| %>
	<li><%= link_to blog.title, blog_path(blog) %> (<%= blog.updated_at.strftime('%d %b') %>)</li>
	<% end %>
</ul>